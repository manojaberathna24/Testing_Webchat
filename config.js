// config.js - Credentials are stored in browser localStorage
// No hardcoded API keys needed here for GitHub Pages deployment
// The chatbot reads API key and model from localStorage (set via admin.html or the settings modal)

// Optionally, you can set a default config here:
// window.CHATBOT_CONFIG = {
//   apiKey: 'sk-or-v1-...',
//   model: 'google/gemma-2-9b-it:free'
// };
